ANTHROPIC_API_KEY=sk-ant-xxx
# Optional: override the per-scenario default model
# ANTHROPIC_MODEL=claude-opus-4-7
# Optional: SQLite path (default: web/data/app.db). Use :memory: for an
# ephemeral DB. Point at a persistent volume in production.
# DATABASE_PATH=./data/app.db
